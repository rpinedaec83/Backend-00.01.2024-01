// Variables
let suma = 0;

// Calcular la suma de los primeros cien números con un bucle for
for (let i = 1; i <= 100; i++) {
    suma += i;
}

// Mostrar resultado
console.log("La suma de los primeros cien números es: " + suma);